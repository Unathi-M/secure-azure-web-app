# Copy this file to app/backend/.env only after Azure SQL exists.
# Do not commit .env. These values contain no password because the application uses Microsoft Entra authentication.
AZURE_SQL_SERVER=<YOUR_SQL_SERVER>.database.windows.net
AZURE_SQL_DATABASE=sqldb-secure-azure-dev
AZURE_SQL_PORT=1433
